# Backend Validators Directory

This directory holds Zod validation schemas for input sanitization and schema verification:
- `auth.validator.js`: Register and login payload schemas.
- `analysis.validator.js`: File upload and analysis query parameters.
