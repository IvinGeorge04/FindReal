# Backend Models Directory

This directory holds Mongoose data schemas and models for FindReal:
- `User.js`: User accounts, hashed passwords, roles, timestamps.
- `Analysis.js`: Media analysis jobs, metadata, provenance, probabilistic confidence scores, limitations, timestamps.
- `AuditLog.js`: Security audit events (logins, rate limits, failures).
